[server]
# In container/Codespaces, native file watching can miss edits.
# Polling ensures Streamlit detects changes and reloads reliably.
fileWatcherType = "poll"
runOnSave = true
headless = true
address = "0.0.0.0"
port = 8501

[browser]
gatherUsageStats = false
